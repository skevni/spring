server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: coreService
          uri: http://localhost:8189
          predicates:
            - Path=/core-service/**
          filters:
            - StripPrefix=1